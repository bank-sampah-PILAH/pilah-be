# PIL-228 backend dependency audit

`GET /api/v1/auth/me` already returns the authenticated user's role. Reuse this
contract for frontend navigation rather than adding a second role/menu API.
Supported roles after the existing PIL-225 dependency are `nasabah`, `pengelola`,
`pengelola_induk`, and `superadmin`.

Created from main and merged `feature/pil-225` (`31afd08`) so the customer roles
and their earlier model/membership dependencies are present. No duplicate
authentication implementation was introduced. The merge includes the dependency
history; the new change is a focused session-role contract test.

Tests cover every supported role, signed-out rejection, role spoofing through
query parameters, omission of password data, and denial of staff team access
for a customer. Backend permissions remain authoritative regardless of the
frontend's visible tabs.

Validation: `python manage.py test` passed all 70 tests on Python 3.12.14 with
the pinned requirements. New test file passes Ruff format/check.
